# Backend API URL
VITE_API_URL=http://localhost:5000

# WebSocket URL (same as backend)
VITE_WS_URL=http://localhost:5000

# Device ID and Token are set automatically via QR code onboarding
# Leave these blank - they will be populated when you scan the QR code from admin dashboard
VITE_DEVICE_ID=
VITE_JWT_TOKEN=
